<ul class="collection">
  {{#each this}}
  <li class="collection-item">
    <div class="meta">
      <b>{{user.username}}</b> on {{format_date created_at}}
    </div>
    <div class="text">
      {{comment_text}}
    </div>
  </li>
  {{/each}}
</ul>